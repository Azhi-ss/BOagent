import { test } from "@playwright/test"

test.describe.skip("Chat Flow - Critical User Journey (Legacy)", () => {
  test("Legacy test - skipped", async () => {
    // Disabled legacy chat tests
  })
})

