# LGBO 算法底层设计缺陷分析与重构方案 (Chem-LGBO)

## 核心发现：LGBO 从连续空间到离散化学空间的"水土不服"

当前的 LGBO 算法在处理全类别型（Fully Categorical）的化学反应数据集（如 Buchwald / Suzuki）时，存在严重的算法层面设计缺陷，导致大模型（LLM）的化学推理能力无法得到有效利用。

具体表现在以下三大致命问题：

---

### 缺陷一：One-Hot 编码下的 Hamming 距离毫无化学意义

- **现状**：在当前的 `_llm_mean_shift` 机制中，算法通过计算候选池中各个配方与 LLM 推荐配方的 **Hamming 距离（不匹配的试剂数量）** 来寻找 K 个最近邻并进行后验均值抬升。
- **问题**：Hamming 距离基于 One-Hot 编码，它完全无视化学结构。例如：从配体 `PCy3` 换成 `XPhos`（化学性质相近的大位阻烷基膦），与从 `PCy3` 换成 `PPh3`（化学性质差异巨大的芳基膦），在算法看来**距离完全一样**（都是 Hamming=1）。
- **后果**：LLM 精心挑选的化学方向，在距离衰减的过程中被纯粹的符号匹配破坏，抬升了可能在化学上完全不相似的配方。

### 缺陷二：数学全排列与化学合法性的冲突

- **现状**：LLM 被要求在 5 个变量（亲电体、亲核体、配体、碱、溶剂）中各选一个，形成一个推荐组合（Point）。提示词没有任何关于配对合法性的约束。
- **问题**：在 Suzuki 数据集中，目标产物是确定的（喹啉-吲唑偶联）。这意味着**必须用喹啉类底物去搭配吲唑类底物**。如果 LLM 盲目推荐了"喹啉类亲电体 + 喹啉类亲核体"，在化学上根本无法生成目标产物。
- **后果**：幸运的是，`searchspace.csv` 候选池（5760 种组合）中本身就不包含这种非法的配对。当 LLM 给出非法配对时，算法会在候选池中强行寻找 Hamming 距离最近的"合法点"。这意味着 LLM 的原始意图被算法扭曲了，LLM 其实是在做"盲人摸象"。

### 缺陷三：Region（区间）模式在类别变量上的全面失效

- **现状**：提示词保留了论文原始的 `Point/Region` 模式。
- **问题**：在离散试剂空间中，不存在"从试剂A到试剂B"的连续区间。目前的解析器代码 (`lgbo_parser.py`) 强制要求 Region 模式下 `lb[i] == ub[i]`，如果不等，则直接判断解析失败（返回 None，该轮退化为纯 GP）。
- **后果**：Region 模式形同虚设，不但白白浪费了 Token 和 LLM 的注意力，也违背了化学家进行探索时的真实直觉（化学家往往倾向于指定一个"类群"，而不是死扣某一个精确的单一配方）。

---

## 解决方案：向 Chem-LGBO 进化 (Chemical Sub-manifold)

为了修复上述问题，我们需要将算法从"基于单个数学点和 Hamming 邻域"重构为"基于化学子空间（Sub-manifold）和集合求交"。

### 1. 提示词重构：从单选 (Point) 变多选 (Subspace)
彻底废弃 Point/Region 概念。允许 LLM 为每个反应变量输出一个**候选数组（子集）**。
```json
{
  "Ligand": ["XPhos", "PCy3"],
  "Base": ["CsF", "K3PO4"],
  ...
}
```
LLM 的作用不再是指定一个点，而是利用化学直觉框定一个**高潜力的化学子空间**。

### 2. 约束前置：引入反应模板
在 System Prompt 的 Background 中明确告知 LLM 反应物的合法搭配规则（如：必须是 喹啉+吲唑 或 吲唑+喹啉），防止前端产生化学常识错误。

### 3. 算法重构：直接子空间抬升 (Subspace Masking)
修改 `lgbo.py` 中的 `_llm_mean_shift` 逻辑。**废弃 One-Hot Hamming 距离计算**。
直接在合法的 `pool_X`（真实的 searchspace 候选池）中，找到被 LLM 子空间覆盖的所有候选配方，然后对这些配方的后验均值施加均匀抬升（或基于 Tanimoto 相似度的平滑抬升）。

**新架构分工**：
- **LLM 做粗筛**：划定符合化学机理的有潜力的试剂类群方向（Subspace）。
- **GP 做精筛**：在合法的子空间交集内，利用统计模型和 UCB 选出具体的单个配方去指导实验。

---

## 延伸佐证：来自顶级学术标杆 CurryBO 的印证

为了验证上述 Chem-LGBO 重构方向的合理性，我们对业界顶级化学 BO 框架 **`currybo`** 进行了源码级剖析（`currybo` 专门用于优化跨底物的 Buchwald/Suzuki 等复杂化学数据集）。

剖析结果从根本上印证了我们的结论：

1. **彻底抛弃 One-Hot，拥抱化学指纹**：
   在 `currybo` 的源码 `discrete_problem.py` 中，所有离散的化学试剂 SMILES 都被转换成了 **1024 维的 Morgan 化学指纹（Morgan Fingerprints）**。GP 模型看到的不再是毫无意义的类别符号，而是真实的分子结构拓扑。这完美解释了为什么我们当前的 One-Hot 编码在化学探索中显得如此吃力。
   
2. **Tanimoto 相似度取代 Hamming 距离**：
   在计算两个分子或反应条件的距离时，`currybo` 显式使用了化学界公认的 **Tanimoto 相似度**（通过比较两个分子指纹中重合的特征片段比例）。这进一步证明了当前 LGBO 中的 Hamming 距离在化学优化中是不合格的。

---

## 架构解耦：消除 LLM 的指纹认知负担

一个常见的疑问是："如果底层变成了 1024 维的化学指纹，大模型还认识吗？"

这正是 Chem-LGBO 架构设计的精妙之处：**前端和后端是完全解耦的。**

### 1. 前端（大模型工作区）：只使用人类可读的化学名
大模型看到的 `options.json` 依然是纯英文的 IUPAC 名称（如 `"tricyclohexylphosphane"`）。它利用预训练语料中的化学知识库，通过名字框定候选子集（Subspace）。它只是告诉后端："请在这个候选池里，帮我把搜索范围缩小到这几款配体。"

### 2. 后端（GP 算法工作区）：机器视角的数学映射
当后端 Python 代码拿到大模型吐出的这些"人话"名字后，LLM 的工作就结束了。后端会自动在 `searchspace.csv` 的有效池中执行**子集求交 (Subspace Masking)**：把不在这个子集里的配方全部剔除。
- 如果继续使用基础的 `One-Hot` 编码，GP 会在这个缩小后的安全子集里运行传统的 UCB 挑选。
- 如果进阶采用 `CurryBO` 的指纹方案，后端会利用 `RDKit` 自动将这些被选中的化学名字翻译成 1024 维的 Morgan 指纹去计算 Tanimoto 概率。

**结论**：改用指纹或者改用子空间（Subspace Masking），完全是底层算法层面的降维打击，对大模型是**完全透明且解耦**的。既解放了底层高斯过程的算力潜能，又确保了大模型的认知负担保持在人类自然语言的维度。
